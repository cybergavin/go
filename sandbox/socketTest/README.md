The socketTest program tests connectivity to a socket and returns "True" or "False" based on whether there is connectivity.

Input Parameters => (1) Protocol - tcp or udp
					(2) Host String - <IP>:<port> or <FQDN>:<port>
					(3) Timeout in seconds